const express=require('express');const controller=require('./ventas-clientes-contactos.controller');const {requireAuth}=require('../../middleware/auth.middleware');const router=express.Router();router.get('/clientes/:id/contactos',requireAuth,controller.list);router.post('/clientes/:id/contactos',requireAuth,controller.create);router.put('/clientes/:id/contactos/:idContacto',requireAuth,controller.update);router.patch('/clientes/:id/contactos/:idContacto',requireAuth,controller.update);router.patch('/clientes/:id/contactos/:idContacto/principal',requireAuth,controller.setPrincipal);router.delete('/clientes/:id/contactos/:idContacto',requireAuth,controller.remove);module.exports=router;
