(function(){
'use strict';
// [Aster | 2026-09-01 | ASTER-MG | FASE 1 LOGISTICA PRODUCCION SEMIAUTOMATICO V001]
// [Aster | 2026-09-01 | ASTER-MG | FASE 3 LOGISTICA PRODUCCION AGREGAR MODO MANUAL V001]
// [Aster | 2026-09-01 | ASTER-MG | FASE 4 LOGISTICA PRODUCCION DETALLE DOBLE MODO V001]
// [Aster | 2026-09-01 | ASTER-MG | FIX LOGISTICA PRODUCCION UX PICKERS CATALOGO CANCEL V001]
// [Aster | 2026-09-01 | ASTER-MG | FIX REESTRUCTURACION LOGISTICA PRODUCCION V001]
// [Aster | 2026-09-01 | ASTER-MG | FIX PVO DOCUMENTOS GESTION 30X30 V002]
const state={
  rows:[],options:[],statuses:[],detail:null,selectedOption:null,optionSearchTimer:null,optionSearchToken:0,
  newMode:'SEMI_AUTOMATICO',newSaving:false,pickerDismissCleanup:null,
  manual:{loaded:false,loading:null,catalogs:null,lists:{project:[],advisor:[],supervisor:[],ppns:[]},selected:{project:null,advisor:null,supervisor:null,ppns:null},timers:{},tokens:{}}
};
const $=id=>document.getElementById(id);
const esc=v=>String(v==null||v===''?'—':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const escRaw=v=>String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const raw=v=>String(v==null?'':v).trim();
const norm=v=>raw(v).toUpperCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'');
const req=(path,options)=>window.ManttoHttp.request(path,Object.assign({cacheTtlMs:0,force:true},options||{}));
const json=(path,method,body)=>req(path,{method,headers:{'Content-Type':'application/json'},body:JSON.stringify(body||{})});
const go=(route,payload)=>window.ManttoRouter&&window.ManttoRouter.go(route,payload||null);
const fmt=v=>v?String(v).slice(0,10):'—';
const STATUS_CATALOG=Object.freeze({area:'Logistica',elemento:'Estatus Produccion'});
function shell(view,title,subtitle,body,actions=''){view.innerHTML=`<div class="lp-page"><section class="lp-card lp-head"><div><p class="lp-eyebrow">Corellian · Logística</p><h1>${esc(title)}</h1><p>${esc(subtitle)}</p></div><div class="lp-actions">${actions}<button class="lp-btn ghost" data-lp-refresh type="button">↻ Actualizar</button></div></section><div id="lp-alert" aria-live="polite"></div>${body}</div>`;}
function alertMsg(message,type='error'){const el=$('lp-alert');if(el)el.innerHTML=`<div class="lp-alert ${type}">${esc(message)}</div>`;}
function clearAlert(){const el=$('lp-alert');if(el)el.innerHTML='';}
function empty(cols,text){return `<tr><td colspan="${cols}" class="lp-empty">${esc(text)}</td></tr>`;}
function indicators(row){return (row.indicadores||[]).map(x=>`<span class="lp-indicator" title="${esc(x.nombre)}">${esc(x.emoji)}</span>`).join('')||'<span class="lp-ok">✓</span>';}
function field(label,value,conflict=false){return `<div class="lp-field ${conflict?'conflict':''}"><small>${esc(label)}</small><strong>${esc(value)}</strong>${conflict?'<em>Valores distintos en FL</em>':''}</div>`;}
function markProductionDirty(){const sync=window.ManttoDataSync;if(!sync||typeof sync.markDirty!=='function')return;['logistica-produccion','logistica-pvo','logistica-documentos'].forEach(route=>sync.markDirty(route));}
async function uploadInitial(id,type,files){for(let i=0;i<files.length;i++){const data=new FormData();data.set('tipo_archivo',type);data.set('numero_archivo',String(i+1));data.set('archivo',files[i]);await req('/api/logistica/produccion/'+id+'/archivos',{method:'POST',body:data});}}
function optionLabel(x){return `${raw(x&&x.id_ppns)||'SIN PPNS'} · ${raw(x&&x.proyecto)||'Sin proyecto'}`;}
function setOptionListOpen(open){const input=$('lp-option-search'),box=$('lp-option-list');if(input)input.setAttribute('aria-expanded',open?'true':'false');if(box)box.hidden=!open;}
function comboOwnsFocus(inputId){const input=$(inputId),combo=input&&input.closest('.lp-combobox');return Boolean(combo&&combo.contains(document.activeElement));}
function catalogEmptyLabel(){return `Sin estatus activos · ${STATUS_CATALOG.area} / ${STATUS_CATALOG.elemento}`;}
function renderStatusOptions(){const select=$('lp-status');if(!select)return;const rows=Array.isArray(state.statuses)?state.statuses:[];select.innerHTML='<option value="">Sin estatus</option>'+rows.map(x=>`<option value="${escRaw(x.id_catalogo)}">${esc(x.articulo)}</option>`).join('')+(rows.length?'':`<option value="" disabled>${escRaw(catalogEmptyLabel())}</option>`);}
function renderSelectedOption(){const x=state.selectedOption;const host=$('lp-auto');if(!host)return;host.innerHTML=x?[field('PPNS',x.id_ppns),field('Proyecto',x.proyecto),field('Asesor',x.asesores),field('Supervisor',x.supervisores),field('Fecha PVO',fmt(x.pvo)),field('PVO FL',x.fechas_pvo_fl),field('Fecha de Cubos',x.fechas_cubos),field('Estatus Logística',x.estatus),field('Disponibilidad',x.ya_registrado?'Ya registrado':'Disponible')].join(''):'';}
function selectOptionById(id){const x=state.options.find(item=>String(item.id_log_ops)===String(id));if(!x)return;state.selectedOption=x;const input=$('lp-option-search');if(input)input.value=optionLabel(x);renderSelectedOption();setOptionListOpen(false);}
function renderOptionList(rows,message='No se encontraron coincidencias.'){const box=$('lp-option-list');if(!box)return;const data=Array.isArray(rows)?rows:[];box.innerHTML=data.length?data.map(x=>`<button class="lp-option${x.ya_registrado?' is-used':''}" type="button" data-lp-option-id="${escRaw(x.id_log_ops)}"><strong>${esc(raw(x.id_ppns)||'SIN PPNS')}</strong><span>${esc(raw(x.proyecto)||'Sin proyecto')}</span><small>#${esc(x.id_log_ops)} · ${x.ya_registrado?'Ya registrado':'Disponible'}</small></button>`).join(''):`<div class="lp-option-empty">${esc(message)}</div>`;setOptionListOpen(comboOwnsFocus('lp-option-search'));box.querySelectorAll('[data-lp-option-id]').forEach(button=>button.onclick=()=>selectOptionById(button.dataset.lpOptionId));}
async function loadOptionData(query=''){const suffix=raw(query)?'?q='+encodeURIComponent(raw(query)):'';const data=await req('/api/logistica/produccion/opciones-ppns'+suffix);state.options=Array.isArray(data.data)?data.data:[];state.statuses=Array.isArray(data.catalogo_estatus)?data.catalogo_estatus:[];renderStatusOptions();return state.options;}
function bindOptionSearch(){const input=$('lp-option-search');if(!input)return;const open=()=>{closePickersExcept('semi');renderOptionList(state.options,'Escribe un PPNS o proyecto para buscar.');};input.onfocus=open;input.onclick=open;input.onkeydown=event=>{if(event.key==='Escape'){event.preventDefault();closeAllPickers();return;}if(event.key==='Tab'){setOptionListOpen(false);return;}if(event.key==='ArrowDown'){const first=$('lp-option-list')?.querySelector('[data-lp-option-id]');if(first){event.preventDefault();first.focus();}}};input.oninput=()=>{state.selectedOption=null;renderSelectedOption();const query=input.value.trim(),token=++state.optionSearchToken;clearTimeout(state.optionSearchTimer);state.optionSearchTimer=setTimeout(async()=>{try{renderOptionList([],'Buscando...');const rows=await loadOptionData(query);if(token!==state.optionSearchToken)return;renderOptionList(rows,query?'No se encontraron PPNS / proyectos.':'No hay filas logísticas disponibles.');}catch(error){if(token!==state.optionSearchToken)return;renderOptionList([],error.message||'No fue posible consultar PPNS / proyectos.');}},220);};}

const MANUAL_PICKERS=Object.freeze({
  project:{input:'lp-manual-project-search',list:'lp-manual-project-list',endpoint:'/api/logistica/produccion/manual/proyectos-vendidos',key:'id_cotizacion'},
  advisor:{input:'lp-manual-advisor-search',list:'lp-manual-advisor-list',endpoint:'/api/logistica/produccion/manual/asesores',key:'id_SB'},
  supervisor:{input:'lp-manual-supervisor-search',list:'lp-manual-supervisor-list',endpoint:'/api/logistica/produccion/manual/supervisores',key:'id_SB'},
  ppns:{input:'lp-manual-ppns-search',list:'lp-manual-ppns-list',endpoint:'/api/logistica/produccion/manual/ppns',key:'id_log_ops'}
});
function manualPickerLabel(kind,row){
  if(kind==='project')return raw(row.nombre_proyecto)||'Proyecto vendido';
  if(kind==='ppns')return raw(row.id_ppns)||'SIN PPNS';
  return raw(row.nombre)||raw(row.iniciales)||'Usuario';
}
function manualPickerSecondary(kind,row){
  if(kind==='project')return [raw(row.id_equipo_vendido)||'Sin PPNS',raw(row.cliente),raw(row.asesor)].filter(Boolean).join(' · ');
  if(kind==='ppns')return [raw(row.proyecto),row.ya_registrado?'Ya registrado':'Disponible'].filter(Boolean).join(' · ');
  return [raw(row.iniciales),raw(row.rol),raw(row.puesto)].filter(Boolean).join(' · ');
}
function setManualPickerOpen(kind,open){const cfg=MANUAL_PICKERS[kind],input=$(cfg.input),box=$(cfg.list);if(input)input.setAttribute('aria-expanded',open?'true':'false');if(box)box.hidden=!open;}
function closeAllPickers(){setOptionListOpen(false);Object.keys(MANUAL_PICKERS).forEach(kind=>setManualPickerOpen(kind,false));}
function closePickersExcept(activeKey){if(activeKey!=='semi')setOptionListOpen(false);Object.keys(MANUAL_PICKERS).forEach(kind=>{if(kind!==activeKey)setManualPickerOpen(kind,false);});}
function pickerKeyFromTarget(target){const combo=target&&target.closest&&target.closest('.lp-combobox');if(!combo)return null;const input=combo.querySelector('[role="combobox"]');if(!input)return null;if(input.id==='lp-option-search')return 'semi';return Object.keys(MANUAL_PICKERS).find(kind=>MANUAL_PICKERS[kind].input===input.id)||null;}
function releasePickerDismiss(){if(typeof state.pickerDismissCleanup==='function')state.pickerDismissCleanup();state.pickerDismissCleanup=null;}
function bindPickerDismiss(view){releasePickerDismiss();const pointerHandler=event=>{if(!view.isConnected)return;closePickersExcept(pickerKeyFromTarget(event.target));};const focusHandler=event=>{if(!view.isConnected)return;closePickersExcept(pickerKeyFromTarget(event.target));};const keyHandler=event=>{if(event.key==='Escape')closeAllPickers();};document.addEventListener('pointerdown',pointerHandler,true);document.addEventListener('focusin',focusHandler,true);document.addEventListener('keydown',keyHandler,true);state.pickerDismissCleanup=()=>{document.removeEventListener('pointerdown',pointerHandler,true);document.removeEventListener('focusin',focusHandler,true);document.removeEventListener('keydown',keyHandler,true);};}
function manualOptionHtml(kind,row){const cfg=MANUAL_PICKERS[kind],key=row[cfg.key];const used=kind==='ppns'&&Number(row.ya_registrado)===1;return `<button class="lp-picker-option${used?' is-used':''}" type="button" data-lp-manual-kind="${kind}" data-lp-manual-key="${escRaw(key)}"><strong>${esc(manualPickerLabel(kind,row))}</strong><span>${esc(manualPickerSecondary(kind,row))}</span>${kind==='project'?`<small>#${esc(row.id_cotizacion)} · ${esc(row.numero_equipos)} equipo(s) · ${esc(row.tipo_equipos)}${raw(row.fecha_cierre)?' · Cierre '+esc(fmt(row.fecha_cierre)):''}</small>`:''}${kind==='ppns'?`<small>#${esc(row.id_log_ops)}${raw(row.pvo)?' · PVO '+esc(fmt(row.pvo)):''}</small>`:''}</button>`;}
function renderManualPickerList(kind,rows,message='No se encontraron coincidencias.'){const cfg=MANUAL_PICKERS[kind],box=$(cfg.list);if(!box)return;const data=Array.isArray(rows)?rows:[];box.innerHTML=data.length?data.map(row=>manualOptionHtml(kind,row)).join(''):`<div class="lp-option-empty">${esc(message)}</div>`;setManualPickerOpen(kind,comboOwnsFocus(cfg.input));box.querySelectorAll('[data-lp-manual-key]').forEach(button=>button.onclick=()=>selectManualPicker(button.dataset.lpManualKind,button.dataset.lpManualKey));}
async function fetchManualPicker(kind,query=''){const cfg=MANUAL_PICKERS[kind],suffix=raw(query)?'?q='+encodeURIComponent(raw(query)):'';const data=await req(cfg.endpoint+suffix);const rows=Array.isArray(data.data)?data.data:[];state.manual.lists[kind]=rows;return rows;}
function renderManualSelectionMeta(kind){const row=state.manual.selected[kind],host=$('lp-manual-'+kind+'-meta');if(!host)return;if(!row){host.textContent=kind==='ppns'?'Puedes escribir un PPNS manual o seleccionar una coincidencia para relacionarlo con Logística.':'Selecciona una opción válida de la lista.';host.className='lp-picker-meta';return;}host.className='lp-picker-meta selected';if(kind==='project')host.textContent=[row.nombre_proyecto,row.id_equipo_vendido?`PPNS ${row.id_equipo_vendido}`:'Sin PPNS de venta',row.cliente].filter(Boolean).join(' · ');else if(kind==='ppns')host.textContent=`Relacionado a log_ops #${row.id_log_ops} · ${row.ya_registrado?'Ya registrado':'Disponible'}`;else host.textContent=[row.iniciales,row.rol,row.puesto].filter(Boolean).join(' · ');}
function singleDate(value){const values=raw(value).split(',').map(x=>x.trim().slice(0,10)).filter(x=>/^\d{4}-\d{2}-\d{2}$/.test(x));return values.length===1?values[0]:'';}
function sourceText(value){return raw(value);}
function applyManualAutoField(inputId,noteId,value,type){const input=$(inputId),note=$(noteId);if(!input||!note)return;const text=sourceText(value);input.disabled=false;input.dataset.auto=text?'1':'0';if(text){if(type==='date')input.value=singleDate(text);else if(type==='status'){const target=norm(text),match=[...input.options].find(o=>norm(o.value)===target||norm(o.textContent)===target);input.value=match?match.value:'';}note.innerHTML=`<span class="lp-source-chip">Precargado por PPNS · editable</span><span>${esc(text)}</span>`;note.className='lp-source-note automatic';return;}note.textContent=state.manual.selected.ppns?'Sin dato automático en el PPNS relacionado; captura manual.':'Captura manual si no relacionas un PPNS.';note.className='lp-source-note';}
function syncManualPpnsFields(){const row=state.manual.selected.ppns;applyManualAutoField('lp-manual-pvo','lp-manual-pvo-note',row&&row.pvo,'date');applyManualAutoField('lp-manual-pvo-fl','lp-manual-pvo-fl-note',row&&row.fechas_pvo_fl,'date');applyManualAutoField('lp-manual-cubos','lp-manual-cubos-note',row&&row.fechas_cubos,'date');applyManualAutoField('lp-manual-log-status','lp-manual-log-status-note',row&&row.estatus,'status');renderManualSelectionMeta('ppns');}
function selectManualPicker(kind,key){const cfg=MANUAL_PICKERS[kind],row=state.manual.lists[kind].find(item=>String(item[cfg.key])===String(key));if(!row)return;state.manual.selected[kind]=row;const input=$(cfg.input);if(input)input.value=kind==='ppns'?(raw(row.id_ppns)||''):manualPickerLabel(kind,row);setManualPickerOpen(kind,false);renderManualSelectionMeta(kind);if(kind==='ppns')syncManualPpnsFields();}
function clearManualSelection(kind){state.manual.selected[kind]=null;renderManualSelectionMeta(kind);if(kind==='ppns')syncManualPpnsFields();}
function bindManualPicker(kind){const cfg=MANUAL_PICKERS[kind],input=$(cfg.input);if(!input)return;const open=()=>{closePickersExcept(kind);renderManualPickerList(kind,state.manual.lists[kind],kind==='ppns'?'Escribe un PPNS para buscar o captúralo manualmente.':'Escribe para buscar.');};input.onfocus=open;input.onclick=open;input.onkeydown=event=>{if(event.key==='Escape'){event.preventDefault();closeAllPickers();return;}if(event.key==='Tab'){setManualPickerOpen(kind,false);return;}if(event.key==='ArrowDown'){const first=$(cfg.list)?.querySelector('[data-lp-manual-key]');if(first){event.preventDefault();first.focus();}}};input.oninput=()=>{if(state.manual.selected[kind])clearManualSelection(kind);const query=input.value.trim(),token=(state.manual.tokens[kind]||0)+1;state.manual.tokens[kind]=token;clearTimeout(state.manual.timers[kind]);state.manual.timers[kind]=setTimeout(async()=>{try{renderManualPickerList(kind,[],'Buscando...');const rows=await fetchManualPicker(kind,query);if(token!==state.manual.tokens[kind])return;renderManualPickerList(kind,rows,kind==='ppns'?(query?'No se encontró el PPNS. Puedes conservar el texto como PPNS manual.':'No hay PPNS disponibles.'):'No se encontraron opciones.');}catch(error){if(token!==state.manual.tokens[kind])return;renderManualPickerList(kind,[],error.message||'No fue posible consultar las opciones.');}},220);};}
function renderManualCatalogs(){const data=state.manual.catalogs||{},prod=Array.isArray(data.estatus_produccion)?data.estatus_produccion:[],log=Array.isArray(data.estatus_logistica)?data.estatus_logistica:[],source=data.catalogo_estatus_produccion||STATUS_CATALOG,label=`${raw(source.area)||STATUS_CATALOG.area} / ${raw(source.elemento)||STATUS_CATALOG.elemento}`;const prodSelect=$('lp-manual-prod-status'),logSelect=$('lp-manual-log-status'),emptyLabel=`Sin estatus activos · ${label}`;if(prodSelect)prodSelect.innerHTML='<option value="">Sin estatus</option>'+prod.map(x=>`<option value="${escRaw(x.id_catalogo)}">${esc(x.articulo)}</option>`).join('')+(prod.length?'':`<option value="" disabled>${escRaw(emptyLabel)}</option>`);if(logSelect)logSelect.innerHTML='<option value="">Sin estatus</option>'+log.map(x=>`<option value="${escRaw(x.valor)}">${esc(x.valor)}</option>`).join('')+(log.length?'':`<option value="" disabled>${escRaw(emptyLabel)}</option>`);}
async function ensureManualData(){if(state.manual.loaded)return;if(state.manual.loading)return state.manual.loading;state.manual.loading=(async()=>{const [catalogs,projects,advisors,supervisors,ppns]=await Promise.all([req('/api/logistica/produccion/manual/catalogos'),fetchManualPicker('project',''),fetchManualPicker('advisor',''),fetchManualPicker('supervisor',''),fetchManualPicker('ppns','')]);state.manual.catalogs=catalogs.data||{};state.manual.lists.project=projects;state.manual.lists.advisor=advisors;state.manual.lists.supervisor=supervisors;state.manual.lists.ppns=ppns;state.manual.loaded=true;renderManualCatalogs();})();try{await state.manual.loading;}finally{state.manual.loading=null;}}
function resetManualNewState(){state.manual.loaded=false;state.manual.loading=null;state.manual.catalogs=null;state.manual.lists={project:[],advisor:[],supervisor:[],ppns:[]};state.manual.selected={project:null,advisor:null,supervisor:null,ppns:null};Object.values(state.manual.timers||{}).forEach(clearTimeout);state.manual.timers={};state.manual.tokens={};}
function setNewMode(mode){state.newMode=mode==='MANUAL'?'MANUAL':'SEMI_AUTOMATICO';document.querySelectorAll('[data-lp-mode]').forEach(button=>{const active=button.dataset.lpMode===state.newMode;button.classList.toggle('active',active);button.setAttribute('aria-pressed',active?'true':'false');});const semi=$('lp-semi-form'),manual=$('lp-manual-form'),copy=$('lp-mode-copy');if(semi)semi.hidden=state.newMode!=='SEMI_AUTOMATICO';if(manual)manual.hidden=state.newMode!=='MANUAL';if(copy)copy.textContent=state.newMode==='MANUAL'?'Captura manual con catálogos relacionados y PPNS opcional.':'Comportamiento actual: selecciona una fila logística y completa datos relacionados automáticamente.';if(state.newMode==='MANUAL')ensureManualData().catch(error=>alertMsg(error.message||'No fue posible cargar el Modo Manual.'));}
function manualAutoPresent(){return false;}
function manualPayload(){const project=state.manual.selected.project,advisor=state.manual.selected.advisor,supervisor=state.manual.selected.supervisor,ppns=state.manual.selected.ppns,ppnsText=raw($('lp-manual-ppns-search')?.value);if(!project)throw new Error('Selecciona un Proyecto vendido válido.');if(!advisor)throw new Error('Selecciona un Asesor válido.');if(!supervisor)throw new Error('Selecciona un Supervisor válido.');if(ppns&&Number(ppns.ya_registrado)===1)throw new Error('El PPNS relacionado ya tiene seguimiento activo de Producción.');if(ppnsText.length>50)throw new Error('PPNS no puede exceder 50 caracteres.');return {modo_registro:'MANUAL',id_cotizacion_venta:project.id_cotizacion,id_log_ops:ppns?ppns.id_log_ops:null,ppns:ppns?raw(ppns.id_ppns):(ppnsText||null),id_asesor:advisor.id_SB,id_supervisor:supervisor.id_SB,fecha_pvo:$('lp-manual-pvo').value||null,fecha_pvo_fl:$('lp-manual-pvo-fl').value||null,fecha_cubos:$('lp-manual-cubos').value||null,estatus_logistica:$('lp-manual-log-status').value||null,id_estatus_produccion:$('lp-manual-prod-status').value||null,comentario:$('lp-manual-comment').value};}
function cancelNewCapture(){if(state.newSaving)return;state.optionSearchToken+=1;clearTimeout(state.optionSearchTimer);Object.values(state.manual.timers||{}).forEach(clearTimeout);Object.keys(state.manual.tokens||{}).forEach(kind=>{state.manual.tokens[kind]=(state.manual.tokens[kind]||0)+1;});closeAllPickers();go('logistica-produccion');}
function setNewSaving(busy){state.newSaving=Boolean(busy);['lp-save','lp-manual-save','lp-cancel','lp-manual-cancel','lp-cancel-new-header'].forEach(id=>{const el=$(id);if(el)el.disabled=state.newSaving;});}

async function loadMain(view){
 shell(view,'Producción','Seguimiento consolidado sin duplicar las fuentes operativas.',`<section class="lp-card lp-toolbar"><input id="lp-q" type="search" placeholder="Buscar proyecto o PPNS"><label><input id="lp-no-pvo" type="checkbox"> Ver sin Fecha PVO</label></section><section class="lp-card"><div class="lp-table-wrap"><table class="lp-table"><thead><tr><th>Docs</th><th>Proyecto</th><th>Asesor</th><th>Supervisor</th><th>Fecha PVO</th><th>PVO FL</th><th>Fecha de Cubos</th><th>Semana/Año</th><th>Comentario</th><th>Estatus Producción</th></tr></thead><tbody id="lp-main-body"></tbody></table></div></section>`,`<button id="lp-add" class="lp-btn primary" type="button">＋ Agregar nuevo</button>`);
 const load=async()=>{try{const p=new URLSearchParams();if($('lp-q').value.trim())p.set('q',$('lp-q').value.trim());if($('lp-no-pvo').checked)p.set('sin_fecha_pvo','1');const data=await req('/api/logistica/produccion?'+p);state.rows=data.data||[];$('lp-main-body').innerHTML=state.rows.length?state.rows.map(r=>`<tr data-detail="${r.id_produccion}"><td>${indicators(r)}</td><td><button class="lp-link" type="button">${esc(r.proyecto)}</button><small>${esc(r.ppns)}</small></td><td>${esc(r.asesores)}</td><td>${esc(r.supervisores)}</td><td>${fmt(r.fecha_pvo)}</td><td>${esc(r.fechas_pvo_fl)}</td><td>${esc(r.fechas_cubos)}</td><td>S${String(r.semana_registro).padStart(2,'0')} / ${r.anio_registro}</td><td>${esc(r.comentario)}</td><td>${esc(r.estatus_produccion)}</td></tr>`).join(''):empty(10,'No hay registros de Producción.');view.querySelectorAll('[data-detail]').forEach(el=>el.onclick=()=>go('logistica-produccion-detalle',{id:el.dataset.detail}));}catch(e){alertMsg(e.message);}};
 $('lp-add').onclick=()=>go('logistica-produccion-nuevo');$('lp-q').oninput=()=>{clearTimeout(state.searchTimer);state.searchTimer=setTimeout(load,250);};$('lp-no-pvo').onchange=load;view.querySelector('[data-lp-refresh]').onclick=load;await load();
}

async function loadNew(view,resetMode=true){
 if(resetMode)state.newMode='SEMI_AUTOMATICO';
 state.newSaving=false;state.selectedOption=null;state.options=[];state.statuses=[];state.optionSearchToken+=1;resetManualNewState();
 shell(view,'Agregar Producción','Elige entre el flujo Semi automático actual o la nueva captura Manual.',`<section class="lp-card lp-mode-card"><div><h2>Modo de captura</h2><p id="lp-mode-copy">Comportamiento actual: selecciona una fila logística y completa datos relacionados automáticamente.</p></div><div class="lp-mode-switch" role="group" aria-label="Modo de captura"><button class="lp-mode-btn active" data-lp-mode="SEMI_AUTOMATICO" aria-pressed="true" type="button">Semi automático</button><button class="lp-mode-btn" data-lp-mode="MANUAL" aria-pressed="false" type="button">Manual</button></div></section>
 <section id="lp-semi-form" class="lp-card lp-form"><div class="lp-form-heading wide"><div><h2>Semi automático</h2><p>Selecciona la fila logística exacta; los datos relacionados son de solo lectura.</p></div></div><label class="wide">PPNS / Proyecto<div class="lp-combobox"><input id="lp-option-search" type="search" autocomplete="off" placeholder="Buscar PPNS o proyecto" role="combobox" aria-autocomplete="list" aria-controls="lp-option-list" aria-expanded="false"><div id="lp-option-list" class="lp-options" role="listbox" hidden></div></div></label><div id="lp-auto" class="lp-fields wide"></div><label>Estatus Producción<select id="lp-status"></select></label><label class="wide">Comentario inicial<textarea id="lp-comment" maxlength="5000" rows="4"></textarea></label><label>CPVO (máximo 2 · 25 MB por archivo)<input id="lp-new-cpvo" type="file" multiple></label><label>GM (máximo 10 · 25 MB por archivo)<input id="lp-new-gm" type="file" multiple></label><p class="lp-policy-note wide">Máximo total: 12 archivos activos. El límite de 25 MB se valida individualmente.</p><div class="lp-form-actions wide"><button id="lp-cancel" class="lp-btn ghost" type="button" title="Descarta esta captura. No elimina, desactiva ni bloquea registros.">Cancelar registro</button><button id="lp-save" class="lp-btn primary" type="button">Crear registro</button></div></section>
 <section id="lp-manual-form" class="lp-card lp-form" hidden><div class="lp-form-heading wide"><div><h2>Manual</h2><p>Proyecto, Asesor y Supervisor se relacionan con datos existentes. PPNS es opcional: si lo relacionas, PVO, PVO FL, Cubos y Estatus Logística toman automáticamente la información disponible.</p></div></div>
 <label class="wide">Proyecto vendido *<div class="lp-combobox"><input id="lp-manual-project-search" type="search" autocomplete="off" placeholder="Buscar proyecto vendido, PPNS, cliente o asesor" role="combobox" aria-autocomplete="list" aria-controls="lp-manual-project-list" aria-expanded="false"><div id="lp-manual-project-list" class="lp-options lp-picker-options" role="listbox" hidden></div></div><small id="lp-manual-project-meta" class="lp-picker-meta">Selecciona una opción válida de la lista.</small></label>
 <label>Asesor *<div class="lp-combobox"><input id="lp-manual-advisor-search" type="search" autocomplete="off" placeholder="Buscar asesor, gerente o director" role="combobox" aria-autocomplete="list" aria-controls="lp-manual-advisor-list" aria-expanded="false"><div id="lp-manual-advisor-list" class="lp-options lp-picker-options" role="listbox" hidden></div></div><small id="lp-manual-advisor-meta" class="lp-picker-meta">Selecciona una opción válida de la lista.</small></label>
 <label>Supervisor *<div class="lp-combobox"><input id="lp-manual-supervisor-search" type="search" autocomplete="off" placeholder="Buscar supervisor o superintendente" role="combobox" aria-autocomplete="list" aria-controls="lp-manual-supervisor-list" aria-expanded="false"><div id="lp-manual-supervisor-list" class="lp-options lp-picker-options" role="listbox" hidden></div></div><small id="lp-manual-supervisor-meta" class="lp-picker-meta">Selecciona una opción válida de la lista.</small></label>
 <label class="wide">PPNS · relación opcional con Logística<div class="lp-combobox"><input id="lp-manual-ppns-search" type="search" maxlength="50" autocomplete="off" placeholder="Escribe un PPNS o busca uno existente" role="combobox" aria-autocomplete="list" aria-controls="lp-manual-ppns-list" aria-expanded="false"><div id="lp-manual-ppns-list" class="lp-options lp-picker-options" role="listbox" hidden></div></div><small id="lp-manual-ppns-meta" class="lp-picker-meta">Puedes escribir un PPNS manual o seleccionar una coincidencia para relacionarlo con Logística.</small></label>
 <div class="lp-manual-source-grid wide"><label>Fecha PVO<input id="lp-manual-pvo" type="date"><small id="lp-manual-pvo-note" class="lp-source-note">Captura manual si no relacionas un PPNS.</small></label><label>PVO FL<input id="lp-manual-pvo-fl" type="date"><small id="lp-manual-pvo-fl-note" class="lp-source-note">Captura manual si no relacionas un PPNS.</small></label><label>Fecha de Cubos<input id="lp-manual-cubos" type="date"><small id="lp-manual-cubos-note" class="lp-source-note">Captura manual si no relacionas un PPNS.</small></label><label>Estatus Logística<select id="lp-manual-log-status"><option value="">Cargando catálogo...</option></select><small id="lp-manual-log-status-note" class="lp-source-note">Captura manual si no relacionas un PPNS.</small></label></div>
 <label>Estatus Producción<select id="lp-manual-prod-status"><option value="">Cargando catálogo...</option></select></label><label class="wide">Comentario inicial<textarea id="lp-manual-comment" maxlength="5000" rows="4"></textarea></label><label>CPVO (máximo 2 · 25 MB por archivo)<input id="lp-manual-cpvo" type="file" multiple></label><label>GM (máximo 10 · 25 MB por archivo)<input id="lp-manual-gm" type="file" multiple></label><p class="lp-policy-note wide">Máximo total: 12 archivos activos. El límite de 25 MB se valida individualmente.</p><div class="lp-form-actions wide"><button id="lp-manual-cancel" class="lp-btn ghost" type="button" title="Descarta esta captura. No elimina, desactiva ni bloquea registros.">Cancelar registro</button><button id="lp-manual-save" class="lp-btn primary" type="button">Crear registro manual</button></div></section>`,`<button id="lp-cancel-new-header" class="lp-btn ghost" type="button" title="Descarta esta captura. No elimina, desactiva ni bloquea registros.">✕ Cancelar registro</button>`);
 renderStatusOptions();bindOptionSearch();Object.keys(MANUAL_PICKERS).forEach(bindManualPicker);bindPickerDismiss(view);
 view.querySelectorAll('[data-lp-mode]').forEach(button=>button.onclick=()=>{clearAlert();setNewMode(button.dataset.lpMode);});
 setNewMode(state.newMode);
 try{await loadOptionData('');}catch(e){alertMsg(e.message);}
 $('lp-cancel').onclick=cancelNewCapture;$('lp-manual-cancel').onclick=cancelNewCapture;$('lp-cancel-new-header').onclick=cancelNewCapture;
 $('lp-save').onclick=async()=>{const x=state.selectedOption,cpvo=[...$('lp-new-cpvo').files],gm=[...$('lp-new-gm').files];if(!x)return alertMsg('Selecciona una opción válida de la lista.');if(x.ya_registrado)return alertMsg('La fila logística ya tiene seguimiento de Producción.');if(cpvo.length>2||gm.length>10)return alertMsg('Solo se permiten 2 archivos CPVO y 10 GM.');setNewSaving(true);let createdId=null;try{const out=await json('/api/logistica/produccion','POST',{modo_registro:'SEMI_AUTOMATICO',id_log_ops:x.id_log_ops,id_estatus_produccion:$('lp-status').value||null,comentario:$('lp-comment').value});createdId=out.data.produccion.id_produccion;markProductionDirty();await uploadInitial(createdId,'CPVO',cpvo);await uploadInitial(createdId,'GM',gm);markProductionDirty();go('logistica-produccion-detalle',{id:createdId});}catch(e){if(createdId){markProductionDirty();window.alert('El registro se creó, pero no todos los archivos pudieron cargarse: '+e.message);go('logistica-produccion-detalle',{id:createdId});}else alertMsg(e.message);}finally{setNewSaving(false);}};
 $('lp-manual-save').onclick=async()=>{const cpvo=[...$('lp-manual-cpvo').files],gm=[...$('lp-manual-gm').files];if(cpvo.length>2||gm.length>10)return alertMsg('Solo se permiten 2 archivos CPVO y 10 GM.');let payload;try{payload=manualPayload();}catch(error){return alertMsg(error.message);}const button=$('lp-manual-save');setNewSaving(true);let createdId=null;try{const out=await json('/api/logistica/produccion','POST',payload);createdId=out.data.produccion.id_produccion;markProductionDirty();await uploadInitial(createdId,'CPVO',cpvo);await uploadInitial(createdId,'GM',gm);markProductionDirty();go('logistica-produccion-detalle',{id:createdId});}catch(error){if(createdId){markProductionDirty();window.alert('El registro manual se creó, pero no todos los archivos pudieron cargarse: '+error.message);go('logistica-produccion-detalle',{id:createdId});}else alertMsg(error.message);}finally{setNewSaving(false);}};
 view.querySelector('[data-lp-refresh]').onclick=()=>loadNew(view,false);
}

function statusOptions(selected){return '<option value="">Sin estatus</option>'+state.statuses.map(x=>`<option value="${x.id_catalogo}" ${String(x.id_catalogo)===String(selected)?'selected':''}>${esc(x.articulo)}</option>`).join('');}
function modeName(mode){return String(mode||'SEMI_AUTOMATICO')==='MANUAL'?'Manual':'Semi automático';}
function fieldSource(label,value,source='',conflict=false){return `<div class="lp-field ${conflict?'conflict':''}"><small>${esc(label)}</small><strong>${esc(value)}</strong>${source?`<span class="lp-field-source">${esc(source)}</span>`:''}${conflict?'<em>Valores distintos en FL</em>':''}</div>`;}
function manualSource(p,key){if(key==='project')return p.id_cotizacion_venta?`Venta #${p.id_cotizacion_venta}`:'Valor guardado en Producción';if(key==='ppns')return p.id_log_ops?`Relacionado a log_ops #${p.id_log_ops}`:'Valor guardado en Producción';return 'Valor guardado en Producción';}
function existingManualSelection(kind,key,fallback){const cfg=MANUAL_PICKERS[kind],rows=state.manual.lists[kind]||[],found=rows.find(row=>String(row[cfg.key])===String(key||''));return found||fallback||null;}
function prepareManualDetailSelections(p,fl){state.manual.selected.project=existingManualSelection('project',p.id_cotizacion_venta,{id_cotizacion:p.id_cotizacion_venta,nombre_proyecto:p.proyecto||'',id_equipo_vendido:p.ppns||'',cliente:'',asesor:''});state.manual.selected.advisor=existingManualSelection('advisor',p.id_asesor,{id_SB:p.id_asesor,nombre:(fl.asesores||[]).join(', '),iniciales:(fl.asesores||[]).join(', '),rol:'Referencia guardada'});state.manual.selected.supervisor=existingManualSelection('supervisor',p.id_supervisor,{id_SB:p.id_supervisor,nombre:(fl.supervisores||[]).join(', '),iniciales:(fl.supervisores||[]).join(', '),rol:'Referencia guardada'});state.manual.selected.ppns=p.id_log_ops?existingManualSelection('ppns',p.id_log_ops,{id_log_ops:p.id_log_ops,id_ppns:p.ppns,proyecto:p.proyecto,pvo:p.fecha_pvo||'',estatus:p.estatus_logistica||'',fechas_pvo_fl:p.fecha_pvo_fl||'',fechas_cubos:p.fecha_cubos||'',ya_registrado:1}):null;}
function semiDetailEditForm(p){return `<form id="lp-edit-form" class="lp-form compact" hidden><div class="lp-detail-mode-note wide"><strong>Modo Semi automático</strong><span>Los valores fueron precargados al crear el registro y quedaron guardados en Producción. El modo de origen no cambia.</span></div><label>Estatus Producción<select id="lp-edit-status">${statusOptions(p.id_estatus_produccion)}</select></label><label>Fecha envío Docs a Fábrica<input id="lp-doc-date" type="date" value="${escRaw(fmt(p.fecha_envio_docs_fabrica)==='—'?'':fmt(p.fecha_envio_docs_fabrica))}"></label><label>Fecha envío Pago a Fábrica<input id="lp-pay-date" type="date" value="${escRaw(fmt(p.fecha_envio_pago_fabrica)==='—'?'':fmt(p.fecha_envio_pago_fabrica))}"></label><label class="wide">Comentario<textarea id="lp-edit-comment" maxlength="5000" rows="4">${escRaw(p.comentario||'')}</textarea></label><div class="lp-form-actions wide"><button id="lp-edit-cancel" class="lp-btn ghost" type="button">Cancelar cambios</button><button class="lp-btn primary" type="submit">Guardar cambios</button></div></form>`;}
function manualDetailEditForm(p){return `<form id="lp-edit-form" class="lp-form compact" hidden><div class="lp-detail-mode-note wide"><strong>Modo Manual</strong><span>Todos los valores se guardan en las mismas columnas de Producción. Relacionar otro PPNS puede precargar datos, pero siguen siendo editables antes de guardar.</span></div><label class="wide">Proyecto vendido *<div class="lp-combobox"><input id="lp-manual-project-search" type="search" autocomplete="off" placeholder="Buscar proyecto vendido, PPNS, cliente o asesor" role="combobox" aria-autocomplete="list" aria-controls="lp-manual-project-list" aria-expanded="false"><div id="lp-manual-project-list" class="lp-options lp-picker-options" role="listbox" hidden></div></div><small id="lp-manual-project-meta" class="lp-picker-meta"></small></label><label>Asesor *<div class="lp-combobox"><input id="lp-manual-advisor-search" type="search" autocomplete="off" placeholder="Buscar asesor, gerente o director" role="combobox" aria-autocomplete="list" aria-controls="lp-manual-advisor-list" aria-expanded="false"><div id="lp-manual-advisor-list" class="lp-options lp-picker-options" role="listbox" hidden></div></div><small id="lp-manual-advisor-meta" class="lp-picker-meta"></small></label><label>Supervisor *<div class="lp-combobox"><input id="lp-manual-supervisor-search" type="search" autocomplete="off" placeholder="Buscar supervisor o superintendente" role="combobox" aria-autocomplete="list" aria-controls="lp-manual-supervisor-list" aria-expanded="false"><div id="lp-manual-supervisor-list" class="lp-options lp-picker-options" role="listbox" hidden></div></div><small id="lp-manual-supervisor-meta" class="lp-picker-meta"></small></label><label class="wide">PPNS · relación opcional con Logística<div class="lp-combobox"><input id="lp-manual-ppns-search" type="search" maxlength="50" autocomplete="off" placeholder="Escribe un PPNS o busca uno existente" role="combobox" aria-autocomplete="list" aria-controls="lp-manual-ppns-list" aria-expanded="false"><div id="lp-manual-ppns-list" class="lp-options lp-picker-options" role="listbox" hidden></div></div><small id="lp-manual-ppns-meta" class="lp-picker-meta"></small></label><div class="lp-manual-source-grid wide"><label>Fecha PVO<input id="lp-manual-pvo" type="date"><small id="lp-manual-pvo-note" class="lp-source-note"></small></label><label>PVO FL<input id="lp-manual-pvo-fl" type="date"><small id="lp-manual-pvo-fl-note" class="lp-source-note"></small></label><label>Fecha de Cubos<input id="lp-manual-cubos" type="date"><small id="lp-manual-cubos-note" class="lp-source-note"></small></label><label>Estatus Logística<select id="lp-manual-log-status"></select><small id="lp-manual-log-status-note" class="lp-source-note"></small></label></div><label>Estatus Producción<select id="lp-manual-prod-status"></select></label><label>Fecha envío Docs a Fábrica<input id="lp-doc-date" type="date" value="${escRaw(fmt(p.fecha_envio_docs_fabrica)==='—'?'':fmt(p.fecha_envio_docs_fabrica))}"></label><label>Fecha envío Pago a Fábrica<input id="lp-pay-date" type="date" value="${escRaw(fmt(p.fecha_envio_pago_fabrica)==='—'?'':fmt(p.fecha_envio_pago_fabrica))}"></label><label class="wide">Comentario<textarea id="lp-edit-comment" maxlength="5000" rows="4">${escRaw(p.comentario||'')}</textarea></label><div class="lp-form-actions wide"><button id="lp-edit-cancel" class="lp-btn ghost" type="button">Cancelar cambios</button><button class="lp-btn primary" type="submit">Guardar cambios</button></div></form>`;}
function hydrateManualDetailForm(p){const project=state.manual.selected.project,advisor=state.manual.selected.advisor,supervisor=state.manual.selected.supervisor,ppns=state.manual.selected.ppns;if($('lp-manual-project-search'))$('lp-manual-project-search').value=project?manualPickerLabel('project',project):raw(p.proyecto);if($('lp-manual-advisor-search'))$('lp-manual-advisor-search').value=advisor?manualPickerLabel('advisor',advisor):'';if($('lp-manual-supervisor-search'))$('lp-manual-supervisor-search').value=supervisor?manualPickerLabel('supervisor',supervisor):'';if($('lp-manual-ppns-search'))$('lp-manual-ppns-search').value=ppns?raw(ppns.id_ppns):raw(p.ppns);renderManualSelectionMeta('project');renderManualSelectionMeta('advisor');renderManualSelectionMeta('supervisor');renderManualSelectionMeta('ppns');renderManualCatalogs();const drafts=[['lp-manual-pvo',p.fecha_pvo],['lp-manual-pvo-fl',p.fecha_pvo_fl],['lp-manual-cubos',p.fecha_cubos]];drafts.forEach(([id,value])=>{const input=$(id);if(!input)return;input.dataset.auto='0';input.disabled=false;input.value=value?String(value).slice(0,10):'';});const logStatus=$('lp-manual-log-status');if(logStatus){logStatus.dataset.auto='0';logStatus.disabled=false;logStatus.value=raw(p.estatus_logistica);}const prod=$('lp-manual-prod-status');if(prod)prod.value=p.id_estatus_produccion==null?'':String(p.id_estatus_produccion);['lp-manual-pvo-note','lp-manual-pvo-fl-note','lp-manual-cubos-note','lp-manual-log-status-note'].forEach(id=>{const note=$(id);if(note){note.textContent='Valor guardado en Producción.';note.className='lp-source-note';}});Object.keys(MANUAL_PICKERS).forEach(bindManualPicker);}
function manualEditPayload(p){const project=state.manual.selected.project,advisor=state.manual.selected.advisor,supervisor=state.manual.selected.supervisor,ppns=state.manual.selected.ppns,ppnsText=raw($('lp-manual-ppns-search')?.value);if(!project||!project.id_cotizacion)throw new Error('Selecciona un Proyecto vendido válido.');if(!advisor||!advisor.id_SB)throw new Error('Selecciona un Asesor válido.');if(!supervisor||!supervisor.id_SB)throw new Error('Selecciona un Supervisor válido.');if(ppns&&Number(ppns.ya_registrado)===1&&String(ppns.id_log_ops)!==String(p.id_log_ops||''))throw new Error('El PPNS relacionado ya tiene seguimiento activo de Producción.');if(ppnsText.length>50)throw new Error('PPNS no puede exceder 50 caracteres.');return {id_cotizacion_venta:project.id_cotizacion,id_log_ops:ppns?ppns.id_log_ops:null,ppns:ppns?raw(ppns.id_ppns):(ppnsText||null),id_asesor:advisor.id_SB,id_supervisor:supervisor.id_SB,fecha_pvo:$('lp-manual-pvo').value||null,fecha_pvo_fl:$('lp-manual-pvo-fl').value||null,fecha_cubos:$('lp-manual-cubos').value||null,estatus_logistica:$('lp-manual-log-status').value||null,id_estatus_produccion:$('lp-manual-prod-status').value||null,comentario:$('lp-edit-comment').value,fecha_envio_docs_fabrica:$('lp-doc-date').value||null,fecha_envio_pago_fabrica:$('lp-pay-date').value||null};}
async function loadDetail(view,id){
 if(!id){shell(view,'Detalle de Producción','No se recibió un registro válido','');return alertMsg('Falta el identificador del registro.');}
 try{
  const [out,opts]=await Promise.all([req('/api/logistica/produccion/'+encodeURIComponent(id)),req('/api/logistica/produccion/opciones-ppns')]);
  state.detail=out.data;state.statuses=opts.catalogo_estatus||[];
  if(String(state.detail.produccion.modo_registro||'SEMI_AUTOMATICO')==='MANUAL'){
   resetManualNewState();await ensureManualData();prepareManualDetailSelections(state.detail.produccion,state.detail.instalaciones||{});
  }
  renderDetail(view);
 }catch(e){shell(view,'Detalle de Producción','No fue posible abrir el registro','');alertMsg(e.message);}
}
function renderDetail(view){const d=state.detail,p=d.produccion,fl=d.instalaciones||{},sale=d.venta||{},files=d.archivos||[];const cpvo=files.filter(x=>x.tipo_archivo==='CPVO'),gm=files.filter(x=>x.tipo_archivo==='GM'),manual=String(p.modo_registro||'SEMI_AUTOMATICO')==='MANUAL';const summary=[fieldSource('Modo',modeName(p.modo_registro),'Origen del registro'),field('Proyecto',p.proyecto),field('Supervisor',(fl.supervisores||[]).join(', ')),field('Asesor',(fl.asesores||[]).join(', ')),field('Semana Registro','S'+String(p.semana_registro).padStart(2,'0')+' / '+p.anio_registro),field('Semana Venta',sale.estado==='AMBIGUO'?'AMBIGUO · '+sale.fechas_candidatas.join(', '):sale.semana?'S'+String(sale.semana.semana).padStart(2,'0')+' / '+sale.semana.anio:'—'),field('Estatus Producción',p.estatus_produccion),field('PPNS',p.ppns),field('Fecha PVO',fmt(p.fecha_pvo)),field('PVO FL',fmt(p.fecha_pvo_fl)),field('Fecha de Cubos',fmt(p.fecha_cubos)),field('Estatus Logística',p.estatus_logistica),field('Comentario',p.comentario)].join('');const editForm=manual?manualDetailEditForm(p):semiDetailEditForm(p);const canInstall=Boolean(raw(p.ppns));shell(view,p.proyecto||'Detalle',`PPNS ${p.ppns||'—'} · Producción · ${modeName(p.modo_registro)}`,`<section class="lp-card"><div class="lp-detail-title"><div><h2>Resumen</h2><span class="lp-mode-badge ${manual?'manual':'semi'}">${esc(modeName(p.modo_registro))}</span></div><button id="lp-edit" class="lp-btn" type="button">✏️ Editar</button></div><div class="lp-fields">${summary}</div>${editForm}</section><section class="lp-card"><div class="lp-detail-title"><div><h2>Zona de carga</h2><p>CPVO ${cpvo.length}/2 · GM ${gm.length}/10</p></div></div><div class="lp-upload-grid">${uploadBlock('CPVO',2,cpvo)}${uploadBlock('GM',10,gm)}</div></section>`,`<button id="lp-install" class="lp-btn" type="button" ${canInstall?'':'disabled title="El registro no tiene PPNS para abrir Instalaciones"'}>🏗️ Ver en Instalaciones</button>`);if(manual)hydrateManualDetailForm(p);bindDetail(view,p.id_produccion);}
function uploadBlock(type,max,files){return `<div class="lp-upload"><h3>${type}</h3><div class="lp-file-list">${files.length?files.map(f=>`<div><span>${type} ${f.numero_archivo} · ${esc(f.nombre_original||f.nombre_archivo)}</span><span>${f.url_acceso?`<a href="${esc(f.url_acceso)}" target="_blank" rel="noopener">Abrir</a>`:''}<button data-file-delete="${f.id_archivo}" type="button">Eliminar</button></span></div>`).join(''):'<p>Sin archivos</p>'}</div><form data-upload="${type}"><label>Slot<select name="numero_archivo">${Array.from({length:max},(_,i)=>`<option value="${i+1}">${type} ${i+1}</option>`).join('')}</select></label><label>Archivo (máximo 25 MB por archivo)<input name="archivo" type="file" required></label><button class="lp-btn" type="submit">＋ Cargar / reemplazar</button></form></div>`;}
function bindDetail(view,id){const p=state.detail.produccion,manual=String(p.modo_registro||'SEMI_AUTOMATICO')==='MANUAL',install=$('lp-install');if(install&&!install.disabled)install.onclick=()=>go('instalaciones-proyectos',{id:p.ppns});$('lp-edit').onclick=()=>{$('lp-edit-form').hidden=false;$('lp-edit').hidden=true;};$('lp-edit-cancel').onclick=()=>renderDetail(view);if(manual)bindPickerDismiss(view);$('lp-edit-form').onsubmit=async e=>{e.preventDefault();let payload;if(manual){try{payload=manualEditPayload(p);}catch(error){return alertMsg(error.message);}}else payload={id_estatus_produccion:$('lp-edit-status').value||null,comentario:$('lp-edit-comment').value,fecha_envio_docs_fabrica:$('lp-doc-date').value||null,fecha_envio_pago_fabrica:$('lp-pay-date').value||null};try{const out=await json('/api/logistica/produccion/'+id,'PATCH',payload);state.detail=out.data;markProductionDirty();if(String(state.detail.produccion.modo_registro||'')==='MANUAL')prepareManualDetailSelections(state.detail.produccion,state.detail.instalaciones||{});renderDetail(view);}catch(err){alertMsg(err.message);}};view.querySelectorAll('[data-upload]').forEach(form=>form.onsubmit=async e=>{e.preventDefault();const data=new FormData(form);data.set('tipo_archivo',form.dataset.upload);try{await req('/api/logistica/produccion/'+id+'/archivos',{method:'POST',body:data});markProductionDirty();const fresh=await req('/api/logistica/produccion/'+id);state.detail=fresh.data;if(String(state.detail.produccion.modo_registro||'')==='MANUAL')prepareManualDetailSelections(state.detail.produccion,state.detail.instalaciones||{});renderDetail(view);}catch(err){alertMsg(err.message);}});view.querySelectorAll('[data-file-delete]').forEach(btn=>btn.onclick=async()=>{if(!confirm('¿Eliminar este archivo del registro?'))return;try{await req('/api/logistica/produccion/'+id+'/archivos/'+btn.dataset.fileDelete,{method:'DELETE'});markProductionDirty();const fresh=await req('/api/logistica/produccion/'+id);state.detail=fresh.data;if(String(state.detail.produccion.modo_registro||'')==='MANUAL')prepareManualDetailSelections(state.detail.produccion,state.detail.instalaciones||{});renderDetail(view);}catch(err){alertMsg(err.message);}});view.querySelector('[data-lp-refresh]').onclick=()=>loadDetail(view,id);}
const LP_TABLE_PAGE_SIZE=30;
function lpPage(rows,page){const data=Array.isArray(rows)?rows:[],pages=Math.max(1,Math.ceil(data.length/LP_TABLE_PAGE_SIZE)),current=Math.min(Math.max(1,Number(page)||1),pages),start=(current-1)*LP_TABLE_PAGE_SIZE;return {rows:data.slice(start,start+LP_TABLE_PAGE_SIZE),page:current,pages,total:data.length,start,end:Math.min(start+LP_TABLE_PAGE_SIZE,data.length)};}
function lpRenderPager(hostId,rows,page,onChange){const host=$(hostId);if(!host)return page;const info=lpPage(rows,page);if(!info.total){host.innerHTML='';return info.page;}host.innerHTML=`<div class="lp-actions"><span>${info.start+1}-${info.end} de ${info.total}</span><button class="lp-btn ghost" type="button" data-lp-page-prev ${info.page<=1?'disabled':''}>← Anterior</button><span>Página ${info.page} de ${info.pages}</span><button class="lp-btn ghost" type="button" data-lp-page-next ${info.page>=info.pages?'disabled':''}>Siguiente →</button></div>`;const prev=host.querySelector('[data-lp-page-prev]'),next=host.querySelector('[data-lp-page-next]');if(prev&&!prev.disabled)prev.onclick=()=>onChange(info.page-1);if(next&&!next.disabled)next.onclick=()=>onChange(info.page+1);return info.page;}
function lpBindDetailActions(view){view.querySelectorAll('[data-detail]').forEach(button=>button.onclick=()=>go('logistica-produccion-detalle',{id:button.dataset.detail}));}
async function loadPvo(view){shell(view,'PVO','Control de integridad: CPVO + PVO Log + PVO FL.',`<section class="lp-card"><h2>PVO completos</h2><div class="lp-table-wrap"><table class="lp-table"><thead><tr><th>Proyecto</th><th>PPNS</th><th>CPVO</th><th>PVO Log</th><th>PVO FL</th><th>Supervisor</th><th>Asesor</th><th>Acción</th></tr></thead><tbody id="lp-pvo-ok"></tbody></table></div><div id="lp-pvo-ok-pager"></div></section><section class="lp-card"><h2>Faltan datos PVO</h2><div class="lp-table-wrap"><table class="lp-table"><thead><tr><th>Proyecto</th><th>PPNS</th><th>CPVO</th><th>PVO Log</th><th>PVO FL</th><th>Falta</th><th>Acción</th></tr></thead><tbody id="lp-pvo-missing"></tbody></table></div><div id="lp-pvo-missing-pager"></div></section>`);let okRows=[],missingRows=[],okPage=1,missingPage=1;const render=()=>{const okInfo=lpPage(okRows,okPage);okPage=okInfo.page;$('lp-pvo-ok').innerHTML=okInfo.rows.length?okInfo.rows.map(pvoRow).join(''):empty(8,'Sin PVO completos.');okPage=lpRenderPager('lp-pvo-ok-pager',okRows,okPage,page=>{okPage=page;render();});const missingInfo=lpPage(missingRows,missingPage);missingPage=missingInfo.page;$('lp-pvo-missing').innerHTML=missingInfo.rows.length?missingInfo.rows.map(r=>{const faltan=[];if(!r.pvo.cpvo)faltan.push('CPVO');if(!r.pvo.pvo_log)faltan.push('PVO Log');if(!r.pvo.pvo_fl)faltan.push('PVO FL');return `<tr><td>${esc(r.proyecto)}</td><td>${esc(r.ppns)}</td><td>${r.pvo.cpvo?'✅':'❌'}</td><td>${r.pvo.pvo_log?'✅':'❌'}</td><td>${r.pvo.pvo_fl?'✅':'❌'}</td><td>${esc(faltan.join(' / '))}</td><td><button class="lp-link" data-detail="${r.id_produccion}">Ver / Gestionar</button></td></tr>`;}).join(''):empty(7,'Sin faltantes PVO.');missingPage=lpRenderPager('lp-pvo-missing-pager',missingRows,missingPage,page=>{missingPage=page;render();});lpBindDetailActions(view);};const load=async()=>{try{const [ok,missing]=await Promise.all([req('/api/logistica/produccion/pvo/completos'),req('/api/logistica/produccion/pvo/faltantes')]);okRows=Array.isArray(ok.data)?ok.data:[];missingRows=Array.isArray(missing.data)?missing.data:[];okPage=1;missingPage=1;render();}catch(e){alertMsg(e.message);}};view.querySelector('[data-lp-refresh]').onclick=load;await load();}
function pvoRow(r){return `<tr><td>${esc(r.proyecto)}</td><td>${esc(r.ppns)}</td><td>✅ ${r.cpvo_count}/2</td><td>✅ ${fmt(r.fecha_pvo)}</td><td>✅ ${esc(r.fechas_pvo_fl)}</td><td>${esc(r.supervisores)}</td><td>${esc(r.asesores)}</td><td><button class="lp-link" data-detail="${r.id_produccion}">Ver / Gestionar</button></td></tr>`;}
async function loadDocuments(view){shell(view,'Documentos de Producción','Consulta documental; la carga y manipulación se realiza en el Detalle de Producción.',`<section class="lp-card lp-toolbar"><input id="lp-doc-q" type="search" placeholder="Buscar Proyecto, PPNS, tipo o documento"></section><section class="lp-card"><h2>Documentos</h2><div class="lp-table-wrap"><table class="lp-table"><thead><tr><th>Proyecto</th><th>PPNS</th><th>Tipo</th><th>Documento</th><th>Fecha de carga</th><th>Usuario</th><th>Acción</th></tr></thead><tbody id="lp-docs"></tbody></table></div><div id="lp-docs-pager"></div></section><section class="lp-card"><h2>Faltan documentos <span class="lp-rule">regla provisional</span></h2><div class="lp-table-wrap"><table class="lp-table"><thead><tr><th>Proyecto</th><th>PPNS</th><th>CPVO</th><th>GM</th><th>Pendiente</th><th>Acción</th></tr></thead><tbody id="lp-docs-missing"></tbody></table></div><div id="lp-docs-missing-pager"></div></section>`);let docRows=[],missingRows=[],docPage=1,missingPage=1;const render=()=>{const docInfo=lpPage(docRows,docPage);docPage=docInfo.page;$('lp-docs').innerHTML=docInfo.rows.length?docInfo.rows.map(r=>`<tr><td>${esc(r.proyecto)}</td><td>${esc(r.ppns)}</td><td>${esc(r.tipo_archivo)} ${r.numero_archivo}</td><td>${esc(r.nombre_original||r.nombre_archivo)}</td><td>${fmt(r.created_at)}</td><td>${esc(r.usuario_iniciales)}</td><td><button class="lp-link" data-detail="${r.id_produccion}">Ver / Gestionar</button></td></tr>`).join(''):empty(7,'Sin documentos.');docPage=lpRenderPager('lp-docs-pager',docRows,docPage,page=>{docPage=page;render();});const missingInfo=lpPage(missingRows,missingPage);missingPage=missingInfo.page;$('lp-docs-missing').innerHTML=missingInfo.rows.length?missingInfo.rows.map(r=>`<tr><td>${esc(r.proyecto)}</td><td>${esc(r.ppns)}</td><td>${r.cpvo_count}/2</td><td>${r.gm_count}/10</td><td>💾 Sin archivos activos</td><td><button class="lp-link" data-detail="${r.id_produccion}">Ver / Gestionar</button></td></tr>`).join(''):empty(6,'Sin faltantes documentales.');missingPage=lpRenderPager('lp-docs-missing-pager',missingRows,missingPage,page=>{missingPage=page;render();});lpBindDetailActions(view);};const load=async()=>{try{const q=$('lp-doc-q').value.trim(),suffix=q?'?q='+encodeURIComponent(q):'';const [docs,missing]=await Promise.all([req('/api/logistica/produccion/documentos'+suffix),req('/api/logistica/produccion/documentos/faltantes'+suffix)]);docRows=Array.isArray(docs.data)?docs.data:[];missingRows=Array.isArray(missing.data)?missing.data:[];docPage=1;missingPage=1;render();}catch(e){alertMsg(e.message);}};$('lp-doc-q').oninput=()=>{clearTimeout(state.searchTimer);state.searchTimer=setTimeout(load,250);};view.querySelector('[data-lp-refresh]').onclick=load;await load();}
async function init(route,payload){releasePickerDismiss();const view=$('view-'+route);if(!view)return;try{if(route==='logistica-produccion')await loadMain(view);else if(route==='logistica-produccion-nuevo')await loadNew(view);else if(route==='logistica-produccion-detalle')await loadDetail(view,payload&&payload.id);else if(route==='logistica-pvo')await loadPvo(view);else if(route==='logistica-documentos')await loadDocuments(view);}catch(e){alertMsg(e.message);}}
window.ManttoLogisticaProduccion={init};
})();
